# Existing tuple with integers
original_tuple = (1, 2, 3, 4)

# Additional integers to add
additional_integers = (5, 6, 7)

# Create a new tuple by concatenating the original tuple and additional integers
new_tuple = original_tuple + additional_integers

print("Original tuple:", original_tuple)
print("New tuple with additional integers:", new_tuple)
